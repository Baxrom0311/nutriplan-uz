# NutriPlan Frontend

## Netlify

Build command: `npm run build`

Publish directory: `dist`

Required environment variable:

`VITE_API_BASE_URL=https://your-backend.onrender.com/api`

`netlify.toml` already contains the SPA redirect for React Router.
